class CtrlMenuStrip;
class Display3DEN {
    class Controls {
        class MenuStrip: CtrlMenuStrip {
            class Items {
                items[] += {QUOTE(PREFIX)};
                class PREFIX {
                    text = CSTRING(Toolbar);
                };
            };
        };
    };
};
